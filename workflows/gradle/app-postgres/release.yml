name: release

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: opengoodio/central-workflows/.github/workflows/gradle-github-release.yml@main
    secrets:
      git-email: ${{ secrets.GIT_EMAIL }}
      git-user: ${{ secrets.GIT_USER }}
      run-gradle-validation: false
